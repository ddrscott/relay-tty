[project]
name = "relay-tty-docs"
version = "0.1.0"
description = "Documentation for relay-tty"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.6",
    "mkdocs-llmstxt-md>=0.2",
    "mkdocs-minify-plugin>=0.8",
]

[tool.hatch.build.targets.wheel]
packages = []
