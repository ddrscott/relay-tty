[project]
name = "relay-tty-tools"
version = "0.1.0"
description = "Development tools for relay-tty"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
screenshots = [
    "playwright>=1.40",
    "Pillow>=10.0",
]

[tool.hatch.build.targets.wheel]
packages = []
